import torch 

class SSD300:
    def __init__(self):
        pass 
    
    def preprocess:
        pass 
    
    def setup():
        self._detector = torch.hub.load('NVIDIA/DeepLearningExamples:torchhub', 'nvidia_ssd', model_math=model_precision).eval()